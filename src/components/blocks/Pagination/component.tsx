import React, { ReactElement } from 'react';
import './style.css';

const Pagination = (): ReactElement => {
  return <div className="pagination-list">1, 2, 3 ...pagination</div>;
};

export default Pagination;
